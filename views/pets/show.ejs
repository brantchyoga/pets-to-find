<main class="container">
  <div class="row addmarginshow">
    <div class="col s6">
      <div class="row black-borderbottom">
        <% if (images === undefined){ %>
          <div class="col s6">
            <img src="/img/noimage.jpg">
          </div>
        <% } else if(Array.isArray(images)){ %>
          <% images.forEach(function(img) { %>
              <div class="col s6">
                <img class="searchimgsize blue-border-round materialboxed marglittle" src="<%= img.$t %>">
              </div>
            <% }); %>
          <% } else { %>
        <div class="col s6">
          <a href="<%= img.$t %>"><img src="<%= img.$t %>"></a>
        </div>
        <%  } %>
      </div>
    </div>
    <div class="col s6 card">
      <div class="card-title">
        <h2>Info</h2>
      </div>
      <div class="card-content">
        <div class="row">
          <div class="col s4 black-bordertop right">
            <% if (Array.isArray(hasBeen)){ %>
              <ol class="show-hasbeen-style">
              <% hasBeen.forEach(function(string) { %>
                <li><%= string.$t %></li>
              <%  }); %>
              </ol>
            <% } else if (hasBeen) { %>
              <p>1. <%= hasBeen.$t %></p>
            <% } else { %>
              <p>Unfortunately some info like whether the animal is housetrained or not is unavailable</p>
          <%  } %>
          </div>
          <div class="col s8 black-bordertop">
            <p>Age: <%= age %></p>
            <p>Breed:
            <% if (Array.isArray(breeds)){ %>
              <ul class="noTopPadMarg">
              <% breeds.forEach(function(breed) { %>
                <li><%= breed.$t %></li>
                <%  }); %>
                </ul>
              <% } else { %>
                <%= breeds.$t %>
            <%  } %>
            </p>
            <p>Mix?: <%= mix %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col s12 card">
      <div class="card-title">
        <h2>Description</h2>
      </div>
      <div class="card-content black-bordertop">
        <p><%= description %></p>
      </div>
    </div>

  </div>
</main>
